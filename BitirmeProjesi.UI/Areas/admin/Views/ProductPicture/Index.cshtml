@model IEnumerable<ProductPicture>
<div class="right_col" role="main">
    <div class="">
        <div class="row" style="display: block;">
            <div class="col-md-12 col-sm-12  ">
                <div class="x_panel">
                    <div class="col-md-6 col-sm-6">
                        <h2>Ürün Resim Yönetimi</h2>
                    </div>
                    <div class="col-md-6 col-sm-6">
                        <a asp-controller="ProductPicture" asp-action="New" asp-route-productid="@ViewBag.ProductId" class="btn btn-success float-right" style="color:#fff">Yeni Ürün Resmi</a>
                    </div>
                    <div class="x_content">
                        <div class="table-responsive">
                            <table class="table table-striped jambo_table bulk_action">
                                <thead>
                                    <tr class="headings">

                                        <th class="column-title">Resim Adı </th>
                                        <th class="column-title">Sıra</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (ProductPicture s in Model)
                                    {
                                        <tr class="even pointer">
                                            <td class=" "><a asp-controller="ProductPicture" asp-action="Edit" asp-route-id="@s.ID">@s.Name</a></td>
                                            <td class=" ">@s.DisplayIndex</td>
                                            <td><a asp-controller="ProductPicture" asp-action="Delete" asp-route-id="@s.ID" class="btn btn-danger">Sil</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>


                    </div>
                </div>
            </div>
        </div>
    </div>
</div>